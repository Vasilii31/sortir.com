<nav class="navbar navbar-expand-lg navbar-dark w-75 m-auto bg-custom-prim px-4">
    <div class="container-fluid">
        <!-- Titre / Logo -->
        <a class="navbar-brand txt-custom-sec" href="{{ path('app_sortie_index') }}">
            <img src="logo.png" alt="Logo sortir" style="width: 100px;">
        </a>

        <!-- Bouton hamburger pour mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Liens de navigation + Auth -->
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                <!-- Liens -->
                <li class="nav-item">
                    <a class="nav-link txt-custom-sec" href="{{ path('app_villes_list') }}">Villes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link txt-custom-sec"  href="{{ path('app_site_index') }}">Sites</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link txt-custom-sec"  href="{{ path('app_sortie_index') }}">Accueil</a>
                </li>

                <!-- Authentification boutons -->
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link a-custom-sec" href="{{ path('app_sortie_index') }}">Mon profil</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="a-custom-sec-outline" href="{{ path('app_sortie_index') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item ms-2">
                        <a class="a-custom-sec " href="{{ path('app_sortie_index') }}">Login</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="a-custom-sec-outline" href="{{ path('app_sortie_index') }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
