<nav class="navbar navbar-expand-lg navbar-dark w-75 m-auto bg-custom-sec py-4 px-4">
    <div class="container-fluid">
        <!-- Titre / Logo -->
        <a class="navbar-brand" href="{{ path('app_home') }}">SORTIR</a>

        <!-- Bouton hamburger pour mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Liens de navigation + Auth -->
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                <!-- Liens -->
                <li class="nav-item">
                    <a class="nav-link txt-custom-prim" href="{{ path('app_villes_list') }}">Villes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_sites_list') }}">Sites</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>

                <!-- Authentification boutons -->
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Mon profil</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-light" href="{{ path('app_home') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-light" href="{{ path('app_home') }}">Login</a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-light" href="{{ path('app_home') }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
